const TodoList = Backbone.View.extend({
    el: '#todo-list', 
    initialize: function () { 
        // Event listeners
        
    },
    render: function() {
        // Render each todo item in the collection
        
    }
})