const TodoList = Backbone.View.extend({
    el: '#todo-list', 
    initialize: function () { 
        // Event listeners
        // console.log('Initialize has fired.')
    },
    render: function() {
        // Render each item in collection
    }
})